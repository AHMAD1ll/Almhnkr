// هذا هو القسم الجديد والمهم
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'Almhnkr'

// هذا السطر يخبر Gradle بالبحث عن الإضافات في node_modules
apply from: new File(rootProject.projectDir, '../node_modules/react-native/react.gradle')

include ':app'
